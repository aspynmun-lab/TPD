import { CreateMeetingScreen } from "@/lib/screens/CreateMeetingScreen";

export const metadata = { title: "회의 만들기 · SyncFlow" };

export default function CreatePage() {
  return <CreateMeetingScreen />;
}
