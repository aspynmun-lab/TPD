import { PreferenceInputScreen } from "@/lib/screens/PreferenceInputScreen";

export const metadata = { title: "가능 입력 · SyncFlow" };

export default function RespondPage() {
  return <PreferenceInputScreen />;
}
